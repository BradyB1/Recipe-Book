<%- include("includes/header") %>

<div class="container py-md-5 container--narrow">

  <%- include("includes/flash") %>
    
    <%- include('includes/profileShared') %>
  
    <div class="list-group">
      <div class="row g-3" >
        <% recipes.forEach(function(recipe){ %>
            <!-- <a href="/recipe/<%= recipe._id %>" class="list-group-item list-group-item-action"> -->
                <!-- <img class="avatar-tiny" src="https://gravatar.com/avatar/b9408a09298632b5151200f3449434ef?s=128"> -->
                <!-- <strong><%= recipe.title %></strong> on <%= recipe.createdDate.getMonth()+1 %>/<%= recipe.createdDate.getDate() %>/<%= recipe.createdDate.getFullYear() %>
              </a> -->
              <div class="col-lg-4 col-md-6 col-12 mb-4">
                <div class="card" style="max-width: 100%;">
                    <div class="card-body">
                        <h5 class="card-title"><strong><%= recipe.title %></strong></h5>
                        <h6 class="card-subtitle mb-2 text-body-secondary"><i>Cook Time: <%= recipe.cook_time %></i></h6>
                        <p class="card-text"><%= recipe.description %></p>
                        <a href="/recipe/<%= recipe._id %>" class="card-link"><button type="button" class="btn btn-secondary">View Recipe</button></a>
                    </div>
                </div>
            </div>
        <% }) %>
      </div>

    </div>
  </div>

  <%- include("includes/footer") %>